<Window x:Class="WPFPoprijonok.Pages.ChangePriory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFPoprijonok.Pages"
        mc:Ignorable="d"
        Title="Изменение приоритета" MaxHeight="150" MaxWidth="250" MinHeight="150" MinWidth="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock>Введите приоритет:</TextBlock>
        <TextBox Name="priority" Grid.Row="1" MinWidth="250"></TextBox>

        <WrapPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button IsDefault="True" Click="btnOk_Click"  MinWidth="60" Margin="0,0,10,0" Background="#FF43DCFE">OK</Button>
            <Button IsCancel="True" MinWidth="60" Click="btnCancel_Click" Background="#FF43DCFE">Отмена</Button>
        </WrapPanel>
    </Grid>
</Window>